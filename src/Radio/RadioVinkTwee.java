package Radio;

import javax.swing.*;
import java.util.jar.JarEntry;

public class RadioVinkTwee {
    public static void main(String[] args) {

        RadioVink rv = new RadioVink();
        rv.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        rv.setSize(300,200);
        rv.setVisible(true);

    }
}
